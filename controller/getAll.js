const ajax = require("./ajax.js");
let sve;
// ajax.getAll().then(res=>{
//     console.log(res);
//     sve = res;
// });
// async function getAndFill() {
//     ajax.getAll().then(res=>{
//         sve = res;
//     });
// }

/*$(document).ready(function() {
    ajax.getAll().then(res=>{
        sve = res;
    });
    let proizvodiTemplate = $("#proizvodi-template").html();
    let compiledProizvodiTemplate = Handlebars.compile(proizvodiTemplate);
    $(".proizvodi_flex").html(compiledProizvodiTemplate({naziv: "lmao"}));
});*/
